// WebSite schema is now included inside OrganizationSchema via @graph.
// This component is kept for backward-compatible imports but renders nothing.
export default function WebSiteSchema() {
  return null;
}
